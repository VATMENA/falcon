{
  "name": "auth",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "db": "workspace:*",
    "tsconfig": "workspace:*"
  },
  "dependencies": {
    "@lucia-auth/adapter-prisma": "^3.0.0",
    "lucia": "^2.0.0"
  }
}
